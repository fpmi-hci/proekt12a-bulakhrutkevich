name: Project CI

on:
  push:
    branches: [ main, development]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.7, 3.8, 3.9]

 
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        
      - name: Build, Push and Deploy to Heroku #set the whatever name you want to this step
        id: heroku
        uses: fpmi-hci/proekt12a-bulakhrutkevich@v1.0.0  # use the latest version of the action
        with:
          email: ${{ secrets.alexekozunov@gmail.com }} # your heroku email
          api_key: ${{ secrets.90b36ca7-c33d-49ea-9555-1110e95a325a }} # your  heroku api key
          app_name: ${{ secrets.roast-beef }} # you aplication name
          docker_compose_file: 'docker-compose.yml'
          options: '' # Docker Build Options
